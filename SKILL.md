---
name: docx-helper
description: 文档排版助手 — 对已有 docx 文档分析并套用规范排版。先分析差分报告、对话确认后应用到版本化新文件；每次修改产生新版本永不覆盖原文件。仅排版已有文档，不创建新文档。
agent_created: true
---

# 文档排版助手 (docx-helper)

对已有的 Word 文档 (.docx) 按规范排版习惯（参考 GB/T 9704-2012）自动调整格式。

## 核心理念

两个设计原则贯穿整个工作流：

1. **先分析后执行**：不直接改文件，先生成差分报告让用户审视每处变更，确认后再应用
2. **版本化输出**：每次排版输出 `原名+docx-helper+v{N}.docx`，原文件永不覆盖，新旧版本链式追溯

## 标准工作流

```
① 分析诊断          ② 对话确认           ③ 应用排版
──────────        ──────────         ──────────
提供原文    →     列出差分报告   →   用户审阅    →   执行排版
                  每段的识别结果       确认/调整      输出 v1.docx
```

### 第一步：分析诊断

运行分析模式，产出结构化差分报告：

```bash
python scripts/format.py --analyze 报告.docx
```

报告包含：
- **页面变更**：当前边距 vs 目标边距
- **封面检测**：自动识别材料/方案/报告的封面页，给出标题、副标题、单位、日期的排版建议
- **编号层级分析**：自动推断文档编号体系，与 GB/T 9704-2012 国标对比
  - 展示国标要求：一、→（一）→ 1. →（1）
  - 标注文档中使用了国标之外的编号（如 ●、1.1、a.）
  - **对每个不合规编号给出具体修改建议**
- **每段识别结果**：检测类型（大标题/一~四级标题/项目符号/正文）、置信度、当前格式→目标格式
- **字体颜色警告**：标注非黑色文字并提醒
- **加粗警告**：标注将移除的加粗
- **存疑段落**：置信度非 high 的段落
- **表格统计**：表格数量、格式变更说明

呈现方式：先展示编号与国标的对比结论和建议，再逐段列出格式变更。

### 第二步：对话确认

用户审阅报告后，可能：

| 用户反馈 | 处理方式 |
|----------|----------|
| "确认，全部应用" | 直接进入第三步 |
| "第 N 段不是一级标题，改成正文" | 记录覆盖规则，进入第三步时应用 |
| "第 N 段不要改" | 记录跳过规则 |
| "附件那段也按正文处理" | 补充覆盖规则 |

如果用户有覆盖规则，将它们整理为 JSON 文件传给 `--overrides`：

```json
{
  "paragraphs": {
    "3": "body",
    "7": "h1"
  }
}
```

### 第三步：应用排版

用户确认后执行：

```bash
# 全部应用（自动分配版本号）
python scripts/format.py --apply 报告.docx

# 带覆盖规则
python scripts/format.py --apply 报告.docx --overrides overrides.json

# 指定版本号
python scripts/format.py --apply 报告.docx --version 3
```

输出文件自动命名：`报告+docx-helper+v1.docx` → `报告+docx-helper+v2.docx` → ...

### 迭代工作流

```
v1: 报告.docx → 分析 → 确认 → 报告+docx-helper+v1.docx
v2: 拿 v1 → 再分析 → 再确认 → 报告+docx-helper+v2.docx
v3: ...
```

每次都基于上一个版本继续调整，链式追溯，随时可回退到任意版本。

## 排版规则

### 优先顺序

- **第一优先**：用户明确提供的"材料排版要求"
- **补充参考**：GB/T 9704-2012《党政机关公文格式》（`references/gbt9704_2012.md`）

### 页面

| 项目 | 值 |
|------|-----|
| 纸张 | A4 (210 × 297 mm) |
| 上边距 | 3.7 cm |
| 下边距 | 3.5 cm |
| 左/右边距 | 2.7 cm |
| 页脚距底边 | 2.54 cm |

### 字体（方正字库）

| 用途 | 字体 | 字号 | 加粗 | 颜色 |
|------|------|------|------|------|
| 大标题 | 方正小标宋_GBK | 二号 (22pt) | 否 | 黑 |
| 一级标题（一、） | 方正黑体_GBK | 三号 (16pt) | 否 | 黑 |
| 二级标题（（一）） | 方正楷体_GBK | 三号 (16pt) | 否 | 黑 |
| 三级标题（1.） | 方正仿宋_GBK | 三号 (16pt) | 否 | 黑 |
| 四级标题（(1)/①） | 方正仿宋_GBK | 三号 (16pt) | 否 | 黑 |
| 正文 | 方正仿宋_GBK | 三号 (16pt) | 否 | 黑 |
| 西文/数字 | Times New Roman | 三号 (16pt) | — | 黑 |
| 页码 | Times New Roman | 四号 (14pt) | — | 黑 |

### 段落

| 项目 | 值 |
|------|-----|
| 正文行距 | 28.9 磅（固定值） |
| 正文首行缩进 | 2 字符 |
| 大标题上方 | 空 2 行 |
| 大标题下方 | 空 1 行后开始正文 |

### 标题识别（多层级编号，以 GB/T 9704-2012 §7.3.3 为准）

国标规定：结构层次序数**依次**用 `一、` → `（一）` → `1.` → `（1）`。四层字体分别为黑体→楷体→仿宋→仿宋。超出四层时国标未作规定。

| 层级 | 编号格式 | 字体 | 合规性 |
|------|---------|------|--------|
| 一级 | `一、二、三、` | 黑体 | ✅ 国标第一层 |
| 二级 | `（一）（二）` | 楷体 | ✅ 国标第二层 |
| 三级 | `1. 2. 3.` | 仿宋 | ✅ 国标第三层 |
| 四级 | `（1）（2）` `①②` | 仿宋 | ✅ 国标第四层 |
| — | `● ◆ ★ ■` | — | ❌ 不在国标内，正式公文不应使用 |
| — | `1.1 4.1.1` | — | ❌ 不在国标内，技术方案可用 |

分析报告会逐段列出每处不合规编号，并给出修改建议（如：`●` 建议改为 `（1）` 格式）。

### 封面排版（材料/方案/报告）

GB/T 9704-2012 不涉及封面，以下为各地政府机关实际使用的惯例：

| 元素 | 字体 | 字号 | 位置 | 间距 |
|------|------|------|------|------|
| 大标题 | 方正小标宋_GBK | 二号 | 居中 | 上方空 7 行（约页面 1/3 处） |
| 副标题 | 方正楷体_GBK | 三号 | 居中，主标题正下方 | 与主标题同行距，不空行，可前加"——" |
| 单位名称 | 方正仿宋_GBK | 三号 | 居中 | 与标题区之间空 5 行 |
| 日期 | 方正仿宋_GBK | 三号 | 居中，单位正下方 | 同行距，不空行 |

封面排版后会在此页末尾自动添加分页符，正文从第二页开始。如果文档不需要封面排版，确认阶段告知即可跳过。

### 页码

- 格式："— N —"（em dash + 页码 + em dash）
- 脚本默认使用**居中**页码（python-docx 对奇偶页不同页脚支持有限）
- 如需精确实现"奇数页右下、偶数页左下"，排版后加载 `docx` 技能，通过 XML 添加 `w:evenAndOddHeaders` + `w:footerReference w:type="even"` 来实现

### 表格

GB/T 9704-2012 对表格内容格式无强制细化规范。脚本使用正文格式（方正仿宋_GBK 三号、无首行缩进）作为默认处理。

| 区域 | 常见做法 | 字号 |
|------|---------|------|
| 表头 | 黑体 | 小四 (12pt) 或 五号 (10.5pt) |
| 表体 | 仿宋/宋体 | 五号 (10.5pt) |

> 如果文档中对表格字号有特殊要求（如"表头黑体小四、表体仿宋五号"），请在确认阶段告知，用覆盖规则或 docx 技能手动调整。

## 精细调整（脚本覆盖不到的场景）

脚本覆盖了页面、字体、行距、页码、表格等常规排版。以下场景需要用系统内置 `docx` 技能手动处理：

- **奇偶页页码位置**：脚本默认居中；需改为奇数页右下、偶数页左下
- 版头元素：红色分隔线、发文机关标志、发文字号、签发人
- 印章位置：发文机关署名 + 成文日期 + 印章布局
- 版记：抄送机关、印发机关和日期、分隔线
- 多行标题的梯形或菱形排列
- 附件的编排格式、页码连续性

加载 `docx` 技能后，用其 unpack → 编辑 XML → repack 工作流进行精细调整。详细规则见 `references/gbt9704_2012.md`。

## 补充规则（来自 GB/T 9704-2012）

当用户材料要求未覆盖时使用。完整内容见 `references/gbt9704_2012.md`。

- 主送机关：标题下空一行，居左顶格，末尾全角冒号
- 附件说明：正文下空一行左空二字"附件："，名称后不加标点
- 成文日期：右空四字，阿拉伯数字标全年月日，不编虚位
- 印章：红色，居中下压署名和日期
- 装订：左侧装订，两钉距上下边缘各 70 mm

## 注意事项

- **仅支持 .docx**：旧版 .doc 需先另存为 .docx
- **字体前置条件**：系统需安装方正字库（方正小标宋/黑体/楷体/仿宋_GBK），否则 Word 会提示字体缺失
- **版本安全**：原文件永不修改，每次输出新版本文件
- **表格处理**：脚本用正文字体但取消首行缩进；如需保留表格特殊格式，排版后手动调整
