---
name: docx-helper
description: 文档排版助手 — 处理 Word 文档格式问题（触发词：排版、公文格式、docx 格式调整、格式审查、GB/T 9704、落款、页码、图片错位等）。新文档用 --new 从内置规范模板创建（格式全对）；存量 docx 用规范排版流程（GB/T 9704-2012）：重置格式 → 判结构 → 确认 → 版本化套用，永不覆盖原文件。支持落款(signature)/图题表题(caption)类型、图片错位修复(--fix-images)、内容质检(--check-content)、自动编号烘焙。内置「规范文档模板」(assets/)。
agent_created: true
---

# 文档排版助手 (docx-helper) — Agent 执行指南

对 Word 文档 (.docx) 按规范排版习惯（参考 GB/T 9704-2012）自动调整格式。

> **执行者定位**：本技能由 **agent（当前对话中的助手）自主执行全部操作**——用户只给出意图（"处理这份文档""检查格式""新建一份规范"），不接触命令行、不写脚本、不手动改文件。所有脚本调用、结构判定、验证、质检、交付由 agent 完成。**不要把操作步骤抛回给用户。**

> **脚本调用方式**：脚本位于技能目录 `scripts/format.py`，agent 调用时用技能目录的绝对路径（如 `~/.workbuddy/skills/docx-helper/scripts/format.py`），通过可用的 Python 解释器（含 python-docx）执行。下文命令示例中的 `scripts/format.py` 均指此路径。

## Agent 执行铁律

1. **用户只给意图，agent 全流程执行**：不要问"你想让我运行什么命令"，直接判断文档性质并执行对应流程。唯一需要问用户的是：涉及**内容层面修改**（改错别字、重排编号、删段落）需先确认——因为内容改动有语义风险，格式改动不需要问。
2. **先重置、再判结构、后套用**：绝不在脏格式上叠加。先用 reset 清空所有手动格式（含自动编号 numPr），再通读全文判定结构，最后按结构套用。
3. **判结构靠理解上下文，不靠编号字符**：段落类型（标题层级）依据全文语义判定。无编号的小节标题（如"编制背景"）是正则的盲区，必须理解上下文。
4. **版本化输出，永不覆盖原文件**：每次输出 `原名+docx-helper+v{N}.docx`，原文件永远不动，新旧版本链式追溯。
5. **排版后必跑质检**：格式套用完成后自动运行内容质检（错别字/叠词/编号异常），把问题项列出，**内容层面修正需用户确认后再动手**。
6. **套用后必做 AI 复核 + 用户验收（双层校验）**：质检之后 agent 通读成品检查结构误判/内容误伤/位置异常/关键观感；但 **AI 复核看不到 Word 渲染效果、且复核的是自己的判定（有确认偏误）**，最终视觉验收必须由用户在 Word 中打开确认——agent 交付时给"重点确认清单"，把用户从查找问题变成确认清单，**不得宣称"已全部确认无误"**。

## 工作流（agent 自主执行）

### 第 0 步：文档分流（判断性质，选路径）

接到文档任务后，**立即判断文档性质**，选择路径：

| 文档性质 | 判断依据 | 走哪条路径 |
|---------|---------|-----------|
| **新文档** | 用户要"新建/起草"一份规范/方案/预案 | 路径 A：从模板创建 |
| **存量脏文档** | 已有内容、格式混乱（字体乱/无缩进/页码错/图片错位等）| 路径 B：完整排版流程 |
| **存量但基本规范** | 已有内容、格式大体正确，只需局部调整 | 路径 C：分析 + 局部调整 |

> 判断标准：**用户说"新建/起草" → A；用户给的是已有文件且抱怨格式 → B；用户只想"检查/审查格式" → 先 C 后决定是否需要 B。**

### 路径 A：新文档 — 从模板创建

用户要新建文档时，直接从内置模板创建（格式全对，零排版）：

```bash
# 用模板创建，--title 替换封面副标题 + 正文大标题的「××××××」占位符
scripts/format.py --new --title "重保方案" --output "宿迁市公共数据平台重保方案.docx"
# 输出：复制 assets/规范文档模板.docx + 标题占位符替换，示例内容保留
# 同时打印模板版本号（vX.X），便于核对模板是否最新
```

创建后**告知用户**：模板包含封面/页码/落款/编号/表格/附件全部规范格式，按模板结构填写内容即可，无需再排版。若用户要求按特定文档类型（应急预案/巡检规范等）生成，可在模板基础上补充对应章节骨架。

> 模板文件 `assets/规范文档模板.docx` 与脚本常量 `TEMPLATE_VERSION` 绑定。**修改模板后必须同步递增版本号**。

### 路径 B：存量脏文档 — 完整排版流程

```
① reset 重置     →  ② 判结构        →  ③ 用户确认      →  ④ apply 套用   →  ⑤ 质检      →  ⑥ AI 复核
─────────         ─────────          ─────────        ─────────         ─────────        ─────────
清空所有手动格式     通读全文判定段落类型  展示结构摘要确认    输出版本化文件      扫描错别字/编号    通读成品检查合理性
(含自动编号烘焙)    (title/chapter/…)   用户调整/放行      v1/v2/… 链式      列表待确认项      结构误判/内容误伤/位置
```

#### ① 重置格式

```bash
scripts/format.py --reset 报告.docx
# 输出：报告+reset.docx（中性文档：仅保留文字与段落顺序，格式全部还原）
```

重置后**必须抽查编号烘焙结果**：用 `--list` 查看段落，若中文序列编号（一、二、三 / （一）（二））烘焙正确（递增而非全"1"），继续；若编号不递增或异常，需人工核对源文档 numId 后再处理。

#### ② 判结构（agent 的核心职责）

读取 reset 文档全文（用 `--list` 输出或直接读文档），**按语义深度而非编号字符**判定每个段落类型。判定规则：

| 类型 | 语义 | 字体 | 判定规则（按深度，不看具体编号字形） |
|------|------|------|------|
| `title` | 大标题 | 小标宋二号 | 文档首页的居中短句大标题 |
| `chapter` | 章 | 黑体三号 | 文档最顶层分章：`一、` `二、` … `九、`，其余层级的父级 |
| `section` | 节 | 楷体三号 | **章的直属子级**——无论写成 `1.` / `2.1` / `（一）`，只要是章下面的第一级子标题 |
| `subsection` | 子节 | 仿宋三号 | **节的直属子级**——如 `4.1.1`、`8.1.1` 等三层编号 |
| `item` | 条目 | 仿宋三号 | `（1）` `（2）` `①` 等带括号/圈号的编号项（通常是一段有内容的编号列表项） |
| `bullet` | 项目符号 | 仿宋三号 | `·` `●` `◆` 等符号引导的短项 |
| `body` | 正文 | 仿宋三号 | 其余——无编号的正文段落、纯文字说明 |
| `signature` | 落款 | 仿宋三号 | 巡检日期/巡检人/签名/日期等落款行——左对齐+固定左缩进（右空约四字），多行同缩进使首字竖直对齐 |
| `caption` | 图题表题 | 黑体三号 | 以「图 2-1」「表 2-1」开头的图题/表题——居中，参考 GB/T 7713.2-2022 |

> **铁律**：不看编号写什么字符，看它在文档树里是"谁的子级"。
> `1.编制背景` 和 `2.1 盘点目标` 同为「章的直属子级」→ 均判 `section`（楷体）；
> `4.1.1 按分层迁移` 是「section 的子级」→ 判 `subsection`（仿宋）；
> `（1）…` 是带编号的内容条目 → 判 `item`（仿宋，无缩进）；
> **数字编号 + 长正文**（如「1. 巡检人员应于一天内完成当次巡检任务…」）→ 判 `body` 而非标题——编号后剩余文字超过 30 字符视为正文条款。

把判定结果写成 `structure.json`（可临时放在工作目录，用完即弃）：

```json
{
  "paragraphs": {
    "0": "chapter",
    "1": "section",
    "3": "section",
    "5": "item",
    "12": "section",
    "16": "subsection"
  },
  "cover": false,
  "title_index": null
}
```

> **旧名兼容**：`h1`/`h2`/`h3`/`h4` 仍可接收，脚本内部自动映射。

#### ③ 用户确认

**把结构判定摘要用一句话告诉用户**（各类型段落数 + 封面判断），让用户放行或调整。这不是"把操作抛回用户"，而是**格式决策的最终确认**——用户可能知道某些段落的特殊语义（如某段是落款而非正文）。格式确定后其余全自动。

> 用户说"按你的判断来"或"没问题" → 直接执行 ④。用户指出某段类型不对 → 改 structure.json 重跑。

#### ④ 套用排版

```bash
# 按结构映射套用（推荐）
scripts/format.py --apply 报告+reset.docx --structure structure.json
# 输出：报告+reset+docx-helper+v1.docx

# 需要微调重跑时指定版本号
scripts/format.py --apply 报告+reset.docx --structure structure.json --version 3
```

不加 `--structure` 时脚本回退到正则启发式（仅兜底，不推荐）。`--overrides` 是旧版兼容参数（覆盖规则 JSON），推荐统一用 `--structure` 结构映射。对于标准编号文档（GB 或 x.y 体系），可让脚本自动判结构再人工复核：

```bash
# 自动检测编号层级 + 排版
scripts/format.py --apply 报告+reset.docx --auto
# 或先生成 structure.json 初稿供审阅
scripts/format.py --auto 报告+reset.docx --output structure.json
```

`--auto` 覆盖约 80% 标准编号文档；不规则文档（大量无编号标题）必须用人工判结构 + `--structure`。

#### ⑤ 质检（apply 后自动做）

排版完成后**自动运行**内容质检，不依赖用户提醒：

```bash
scripts/format.py --check-content 报告+reset+docx-helper+v1.docx
# 输出：{"typos": [...], "numbering_issues": [...]}
```

- 常见错别字库：一但→一旦、防范未然→防患未然、日常日常→日常、任意任意→任意（可经 `.docx-helper.json` 的 `typo_rules` 扩展）
- 编号异常：同级编号重复、跳号、顺序错乱（如 （1）（2）（4）（3）），支持 （1）（2）、1. 2.、一、二、 三种格式

**处理方式**：把报告中的问题**列给用户确认**——错别字/编号这类内容修改有语义风险，需用户点头后 agent 修正（修改后再出 v+1 版本）。格式层面的问题（页面/字体/页码）已在 ④ 自动修正，无需确认。

#### ⑥ AI 复核 + 用户验收（双层校验）

格式调整是确定性规则，但**规则的输入（结构判定）由 AI 做出**——若某段被误判类型，脚本会"忠实地执行错"。因此 apply + 质检后，需要**双层校验**：

**第一层：AI 过程复核（agent 必做）**——通读成品文档，检查四类问题：

| 复核点 | 检查方式 | 发现问题后的处理 |
|--------|---------|----------------|
| **结构误判** | 抽查标题/落款/图题段落：类型与语义是否匹配（如落款排成了正文缩进、图题未居中）| 修正 structure.json 重跑 apply（v+1）|
| **内容误伤** | reset 后编号烘焙是否正确、特殊字符/表格文字是否丢失 | 对比原文，异常时手工恢复后重跑 |
| **位置异常** | 图片/表格数量与原文一致、图片已嵌入型、落款在右下区域 | 图片缺失→补回；落款错位→修正 |
| **关键观感** | 标题是否孤悬页尾、封面后是否分页、页码奇偶是否正确 | 微调（分页/段落属性）后出 v+1 |

> **复核定位**：**轻量通读**，不是从头精读——聚焦高风险点（每章开头、文末落款、图表格、结构判定有疑的段落）。AI 复核保证"规则执行正确"。

**第二层：用户最终验收（交付时明确请用户确认）**——AI 复核有两大盲区，无法替代用户打开 Word 查看：
1. **视觉渲染层**：标题孤悬页尾、表格跨页断裂、图片实际叠压观感、落款"巡"字是否真对齐——这些只在 Word 渲染后可见，XML 里感知不到
2. **确认偏误**：AI 复核的是自己判定的结构，天然倾向认为自己判对了（本技能落款曾返工 7 版，最终靠用户看截图纠正）

> **交付方式**：AI 复核把能自动发现的问题全部消灭后，交付时给用户一份"**请重点确认清单**"（如"请确认附件一落款两行'巡'字对齐"），把用户从"查找问题"变成"确认清单"——用户浏览确认即可，无需逐项排查。
>
> **分工原则**：格式小问题（孤行、对齐、分页）AI 直接修出 v+1；内容/结构层面问题（语义不通、段落归属错误）列给用户确认后再改；**最终视觉验收必须由用户在 Word 中打开确认**，agent 不得宣称"已全部确认无误"。

### 路径 C：存量基本规范 — 分析 + 局部调整

用户说"检查/审查格式"时先走此路径：

```bash
scripts/format.py --analyze 报告.docx
# 输出 JSON 差分报告（页面设置/字体/字号/行距/页码 vs 规范），不修改文件
```

**agent 解读报告后给用户结论**（哪些合规、哪些不合规、合规项是什么），然后问用户是否需要执行完整排版（路径 B）。不要只把 JSON 丢给用户。

## 专项处理（按需使用）

### 图片错位修复

文档出现"正文从图片上穿过/图文重叠"时（原文档用了"衬于文字下方"环绕）：

```bash
scripts/format.py --fix-images 报告.docx
# 输出：报告+fix.docx（浮动图片 anchor → 嵌入 inline，段落居中，原文件不动）
```

> apply 排版时也会自动执行图片修复。排查法见 `references/case-studies.md` 案例 1。

### 落款 / 图题判定

- **落款行**（巡检日期/巡检人/签名/日期）：判 `signature`——左对齐 + 固定左缩进（右空约四字），多行落款用**相同左缩进**使首字竖直对齐，比右对齐可靠（右对齐时半角空格宽度依赖字体度量，易错位）。
- **图题/表题**（图 2-1…/表 2-1…）：判 `caption`——黑体三号居中（图题图下、表题表上，GB/T 7713.2-2022）。
- **附件标题**（如「×××××××表」）：判 `section` 或手动设黑体三号居中（国标附件标题用黑体）。

### 迭代与回退

```
v1: 报告.docx → reset → 判结构 → 确认 → 报告+reset+docx-helper+v1.docx
v2: 微调 structure.json / 内容修正 → 报告+reset+docx-helper+v2.docx
```

每次都基于同一份 reset 文档继续调整，链式追溯，随时可回退到任意版本。

## 排版规则

**排版规则以 `references/rules.md` 为唯一依据**——每一类文档内容（页面/标题/正文/封面/图题表题/落款/表格/页码/编号）在规则文件中都有唯一规则。排版判断一律以规则文件为准，不追溯来源、不区分"图片规则/补充规则"。可执行版为 `docx-helper-default.json`（与规则文件一一对应）。

### 核心规则（速查）

- **页面**：A4，上 3.7cm / 下 3.5cm / 左右 2.7cm，页脚距底边 2.54cm
- **标题**：2 号方正小标宋_GBK（大标题上方空 2 行、下方空 1 行）
- **正文行距**：28.9 磅（固定值），首行缩进 2 字符
- **字体体系**：一级标题 3 号方正黑体_GBK，二级标题 3 号方正楷体_GBK，正文 3 号方正仿宋_GBK
- **西文/数字**：一律 3 号 Times New Roman
- **页码**：奇数页右下角、偶数页左下角，4 号 Times New Roman，格式 `— N —`

### 关键字体映射（速查）

| 结构类型 | 字体 | 字号 |
|---------|------|------|
| `title` | 方正小标宋_GBK | 二号 (22pt) |
| `chapter` | 方正黑体_GBK | 三号 (16pt) |
| `section` | 方正楷体_GBK | 三号 (16pt) |
| `subsection`/`item`/`body` | 方正仿宋_GBK | 三号 (16pt) |
| `signature` | 仿宋（复用 body） | 三号 (16pt) |
| `caption` | 黑体（复用 chapter） | 三号 (16pt) |
| 西文/数字 / 页码 | Times New Roman | 三号 / 四号 |

### 标题识别（语义深度判定，不按编号字形匹配）

GB/T 9704-2012 §7.3.3 的四层序数体系（`一、`→`（一）`→`1.`→`（1）`）是常见编号惯例，但不是唯一写法。实际文档中的 `1.编制背景` 和 `2.1 盘点目标` 应判为同类（`section` 节标题，楷体），`4.1.1` 判为 `subsection`（子节标题，仿宋）。

| 层级 | 编号格式 | 合规性 |
|------|---------|--------|
| 一级 | `一、二、三、` | ✅ 国标第一层（黑体） |
| 二级 | `（一）（二）` | ✅ 国标第二层（楷体） |
| 三级 | `1. 2. 3.` | ✅ 国标第三层（仿宋） |
| 四级 | `（1）（2）` `①②` | ✅ 国标第四层（仿宋） |
| — | `● ◆ ★ ■` | ❌ 不在国标内，正式公文不应使用 |
| — | `1.1 4.1.1` | ❌ 不在国标内，技术方案可用 |

### 封面 / 图题表题 / 表格 要点

- **封面**：大标题小标宋二号居中（上空 7 行）、副标题楷体三号、单位仿宋三号、日期仿宋三号——主标题与副标题同字号仅当两行都是主标题组成部分
- **图题/表题**（caption）：图题图下居中、表题表上居中，黑体三号（GB/T 7713.2-2022）
- **表格**：脚本默认正文格式（仿宋三号、无首行缩进）；表头黑体小四/表体仿宋五号为常见做法，有特殊要求需在确认阶段告知

## 精细调整（脚本覆盖不到的场景）

以下场景需用 python-docx 直接操作或系统 `docx` 技能处理：

- **段内混排（第X条前缀楷体 + 内容仿宋）**：法规条文段（如「第一条　公共数据开放主体应当按照…」）中，条号前缀用楷体、条款内容用仿宋，属于**同一段落内两个 run 的字体差异**。脚本的整段样式覆盖不了，需用 python-docx 拆 run：找到条号前缀（`第[一二三四五六七八九十百]+条`）所在 run，把前缀 run 的 eastAsia 设为楷体、内容 run 设为仿宋，字号统一三号。实测流程见 `references/case-studies.md`。
- **版头元素**：红色分隔线、发文机关标志、发文字号、签发人
- **印章位置**：发文机关署名 + 成文日期 + 印章布局
- **版记**：抄送机关、印发机关和日期、分隔线
- 多行标题的梯形或菱形排列
- 附件的编排格式、页码连续性

> 相关编排规则见 `references/rules.md` 第十节「补充规则」。

## 自定义配置

可通过 `.docx-helper.json` 覆盖任意排版参数（不改的部分自动沿用 GB 默认）。配置查找优先级：`--config` 显式指定 > 当前目录 `.docx-helper.json` > `~/.workbuddy/docx-helper.json` > 默认配置。

配置示例（只改想改的，其余继承默认）：

```json
{
  "fonts": {
    "section": "思源宋体",
    "body": "思源宋体"
  },
  "sizes": {
    "body": 14
  },
  "spacing": {
    "body_indent_chars": 2
  },
  "typo_rules": {
    "部署": "布署",
    "截止": "截至"
  }
}
```

> `typo_rules`：`--check-content` 的自定义错别字库，**合并**到内置库使用（格式：`{"错误写法": "正确写法"}`）。适用于本单位的领域术语/惯用错误词。若用户提到单位特有的易错词，agent 可主动提出写入配置。

完整默认配置见项目根目录 `docx-helper-default.json`。

## 交付规范（每次排版完成必做）

1. **运行质检**：`--check-content` 扫描最终版本，确保无遗漏格式/内容问题。
2. **AI 过程复核**：通读成品，按「⑥ AI 复核 + 用户验收」四类复核点检查合理性，发现格式小问题直接修出 v+1。
3. **给出重点确认清单**：AI 复核后交付一份"请重点确认清单"（如"请确认附件一落款'巡'字对齐""请确认组织架构图位置"），把用户从查找问题变成确认清单。
4. **汇总改动**：向用户说明"改了什么"（页面/字体/行距/页码/图片/编号等，对照修改前后）。
5. **给出文件**：用文件展示工具交付 `原名+reset+docx-helper+v{N}.docx`，并注明原文件未动。
6. **请用户最终验收**：明确告知用户打开 Word 确认渲染效果（孤行/孤页/图片叠压等 AI 看不到的问题），**agent 不得宣称"已全部确认无误"**——最终视觉验收由用户完成。
7. **列出待确认项**：质检 + AI 复核发现的内容层面问题（错别字、编号重复、结构误判）**列给用户确认**，用户点头后修正出下一版；格式问题无需确认。
8. **提醒字体依赖**：文档使用方正字库（小标宋/黑体/楷体/仿宋_GBK），本机未装会提示字体缺失。

## 注意事项

- **仅支持 .docx**：旧版 .doc 需先另存为 .docx（agent 遇到 .doc 时直接提示用户转存，不要尝试处理）
- **自动编号**：reset 会把 Word 自动编号烘焙为正文文本。中文序列（一、二、三 / （一）（二））烘焙应正确递增（chineseCounting/japaneseCounting 已支持）；编号不递增或全为"1"说明源文档 numId 异常，需人工核对
- **图片**：apply 排版与 `--fix-images` 都会把浮动图片（anchor）转为嵌入型（inline），避免图文错位；图片段落自动居中
- **落款对齐**：多行落款（巡检日期+巡检人）用相同左缩进保证首字对齐，勿改用右对齐+空格（半角空格宽度依赖字体度量，易错位）
- **注的编号对齐**：多行注（注：1.…/2.…）应合并为单段落+软换行+悬挂缩进（left=4字符+firstLine=-2字符），勿用全角空格手凑
- **运行环境**：需 Python 3.8+ 与 python-docx（`pip install -r requirements.txt`）

## 参考文档

- `references/rules.md` — **排版规则（唯一依据）**：页面/字体/段落/标题识别/封面/图题/落款/表格/页码/编号/补充规则，可执行版为 `docx-helper-default.json`
- `references/case-studies.md` — **实战案例库**：图文错位/落款对齐/编号烘焙/注对齐/条款混排等踩坑经验与解法，处理同类文档前先读
